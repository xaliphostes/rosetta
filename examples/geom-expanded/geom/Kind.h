#pragma once

// Stock C++ — no rosetta annotations, no rosetta include. This header builds
// with an ordinary C++17 compiler. Any metadata lives out of line (none here).
enum class Kind { Point, Line, Surface, Volume };
