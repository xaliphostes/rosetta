# Basic python binding using Rosetta

## Compile

- Create a `build` directory and go inside
- Type `cmake ..`
- Type `cmake --build . --config Release`

## Test

In the same `build` directory, type

`PYTHONPATH=. python3 ../test.py`