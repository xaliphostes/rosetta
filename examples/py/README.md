# Compiling and testing the examples

**NOTE**: The CMakeFiles.txt is configured for Python 3.14 (toward line 40). Make your change if necessary...

From this folder:
```sh
mkdir build 
cd build
```
Then
```sh
cmake .. 
make
make run
```
